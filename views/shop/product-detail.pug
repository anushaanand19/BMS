extends ../layouts/main-layout.pug

block styles
    link(rel="stylesheet", href="/css/shop.css")

block content
    .product-nav
        .products
            .detailproductdisplay
                img.detailImgProd(src=`${product.imageURL}`, alt=`${product.name}` )
                .product-attr
                    .prdname #{product.title} 
                    .prdauthor  by #{product.author}
                    .detailslabel.prdprice  ₹#{product.price}
                    .detailslabel.prddescription  #{product.description}